import s from './MainPage.module.scss';

const MainPage = () => {
  return (
    <div className={s.mainPage}>
      <div className={`container ${s.wrapper}`}>MainPage</div>
    </div>
  );
};

export default MainPage;
